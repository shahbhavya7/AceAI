import AuthForm from '@/components/AuthForm'
import React from 'react'

const page = () => {
  return <AuthForm type="sign-in"/> // we are using AuthForm component here to render the form for sign in page
}

export default page